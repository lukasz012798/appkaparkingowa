import getData from "./getData";
import fetchData from "./fetchData";

export default updateData = async () => {
  await fetchData();
  //const data = await getData();
  //const sectors = await divideIntoSectors(data);
  //watchPositionCallback(location, sectors);
};
